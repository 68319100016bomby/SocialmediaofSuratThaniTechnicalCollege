@extends('layouts.app') @section('title','ค้นหา — STC Connect') @section('content')<div class="page-head"><div><small>ค้นหาทั่วทั้งวิทยาลัย</small><h1>{{ $term?'ผลการค้นหา “'.$term.'”':'ค้นหา' }}</h1></div></div>@if(!$term)<div class="empty"><b>ค้นหาเพื่อน โพสต์ หรือกลุ่ม</b><p>พิมพ์คำค้นหาที่แถบด้านบน หรือค้นด้วย #แฮชแท็ก</p></div>@else<section class="search-section"><h2>สมาชิก <span>{{ $users->count() }}</span></h2><div class="search-results">@forelse($users as $user)<article><span class="avatar blue">{{ $user->initials() }}</span><div><b>{{ $user->name }}</b><small>{{ $user->department }}</small></div><form action="{{ route('friends.request',$user) }}" method="post">@csrf<button class="btn light">เพิ่มเพื่อน</button></form></article>@empty<p class="muted">ไม่พบสมาชิก</p>@endforelse</div></section><section class="search-section"><h2>กลุ่ม <span>{{ $groups->count() }}</span></h2><div class="search-results">@foreach($groups as $group)<article><span class="group-mini">{{ mb_substr($group->name,0,2) }}</span><div><b>{{ $group->name }}</b><small>{{ $group->description }}</small></div><a class="btn light" href="{{ route('groups.show',$group) }}">เปิดดู</a></article>@endforeach</div></section><section class="search-section"><h2>โพสต์ <span>{{ $posts->count() }}</span></h2>@foreach($posts as $post)@include('partials.post',['post'=>$post])@endforeach</section>@endif @endsection
