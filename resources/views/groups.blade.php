@extends('layouts.app') @section('title','กลุ่ม — STC Connect') @section('content')<div class="page-head"><div><small>ชุมชนในวิทยาลัย</small><h1>กลุ่ม</h1></div><button class="btn primary" data-open="create-group">＋ สร้างกลุ่ม</button></div><div class="group-grid">@foreach($groups as $group)<article class="group-card"><div class="group-cover"><span>{{ mb_substr($group->name,0,2) }}</span></div><div><h3>{{ $group->name }}</h3><p>{{ $group->description }}</p><small>{{ number_format($group->members_count) }} สมาชิก · {{ $group->is_private?'กลุ่มส่วนตัว':'กลุ่มสาธารณะ' }}</small><a class="btn primary wide" href="{{ route('groups.show',$group) }}">เปิดดูกลุ่ม</a></div></article>@endforeach</div>
@push('modals')<dialog id="create-group"><form class="dialog-form" action="{{ route('groups.store') }}" method="post">@csrf<header><b>สร้างกลุ่มใหม่</b><button type="button" data-close>×</button></header><label>ชื่อกลุ่ม<input name="name" required maxlength="120"></label><label>คำอธิบาย<textarea name="description" rows="4"></textarea></label><label class="check"><input type="checkbox" name="is_private" value="1"> กลุ่มส่วนตัว (สมาชิกเท่านั้นที่เห็นโพสต์)</label><button class="btn primary wide">สร้างกลุ่ม</button></form></dialog>@endpush @endsection
